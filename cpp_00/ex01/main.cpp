#include <Contact.hpp>

int main(int argc, char **argv)
{
    while (1)
    {
        if (argv[1] == "EXIT")
            return (0);
        if (argv[1] == "ADD")
        

        if (argv[1] == "SEARCH")
    }
    Contact contacto;
    contacto.setFirstName("semen");
    contacto.setLastName("semen");
    contacto.setPhoneNumber("semen");
    contacto.setDarkestSecret("semen");
    Contact contacto2 = Contact("Jokin", "Fernandez");
}